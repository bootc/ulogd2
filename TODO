o autoconf: check for logmark
o PGSQL: use binary encoding
o PGSQL: use truly async interface
o NFCT: separate out cache handling into NFCTCACHE plugin
o implement Y-switch in stack
o IPFIX exporter plugin
o implement lex/yacc parser

- add support for capabilities to run as non-root
- support for static linking
- issues with ulogd_BASE and partially copied packets (--ulog-cprange)
- problem wrt. ulogd_BASE and fragments
- autoconf detection of SCTP / DCCP support

